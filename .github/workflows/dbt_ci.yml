name: dbt CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  dbt-ci:
    runs-on: ubuntu-latest

    services:
      postgres:
        image: postgres:14
        env:
          POSTGRES_USER: postgres
          POSTGRES_PASSWORD: postgres
          POSTGRES_DB: hr_db
        ports:
          - 5432:5432
        options: >-
          --health-cmd pg_isready
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5

    steps:
      - name: Checkout repo
        uses: actions/checkout@v4

      - name: Set up Python 3.11
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Cache pip dependencies
        uses: actions/cache@v4
        with:
          path: ~/.cache/pip
          key: ${{ runner.os }}-pip-dbt-postgres
          restore-keys: ${{ runner.os }}-pip-

      - name: Install dbt-postgres
        run: pip install dbt-postgres

      - name: Create dbt profiles.yml
        run: |
          mkdir -p ~/.dbt
          cat > ~/.dbt/profiles.yml << 'EOF'
          hr_dbt:
            target: dev
            outputs:
              dev:
                type: postgres
                host: localhost
                port: 5432
                user: postgres
                password: postgres
                dbname: hr_db
                schema: public
                threads: 4
          EOF

      - name: Find dbt project directory
        run: |
          DBT_PROJECT_DIR=$(dirname $(find . -name "dbt_project.yml" | head -1))
          echo "DBT_PROJECT_DIR=$DBT_PROJECT_DIR" >> $GITHUB_ENV
          echo "Found dbt project at: $DBT_PROJECT_DIR"

      - name: dbt debug
        run: dbt debug --project-dir $DBT_PROJECT_DIR

      - name: dbt seed (load CSV files into hr_db)
        run: dbt seed --project-dir $DBT_PROJECT_DIR

      - name: dbt run (build all models)
        run: dbt run --project-dir $DBT_PROJECT_DIR

      - name: dbt test (run all data quality tests)
        run: dbt test --project-dir $DBT_PROJECT_DIR
